# Tier 1 bounded AIOps Agent 驗證摘要

- Scope／risk／upstream source：R2；依 Tier 1 checkpoint 建立可部署至既有 EC2、讀取安全 application JSONL 並提出人工批准建議的最小 AIOps Agent。
- Architecture：`IncidentAnalyzer` 只建立 bounded sanitized facts；`BedrockIncidentAdvisor` 是可抽換 adapter，runtime entrypoint 固定讀取 `/var/log/co-story/application.jsonl`，不新增 CloudWatch read IAM 或常駐 AWS resource。
- Baseline：Backend `330 passed, 8 skipped`。
- Red commits：`bea8e08`（analyzer／adapter）、`8e6b7cb`（unhashable forged values）、`ac15c71`（EC2 entrypoint）、`f0d268f`（固定 runtime env 五鍵讀取）。
- Green commits：`ba9b553`、`069b939`、`a8d0bf3`。
- Input boundary：最多讀取最後 `200` 行，只接受 request／Storyteller exact allowlist schema；malformed、額外欄位、query path 與不合型別事件均丟棄，不把 request ID 或 raw line 傳給模型。
- Output boundary：固定五欄 JSON；action 只能為 `NO_ACTION`、`RUN_HEALTH_CHECK`、`RESTART_APPLICATION`、`CHECK_DATABASE`，且 `requires_human_approval` 必須為 `true`；Agent 本身沒有執行修復工具。
- Model boundary：單次 guarded Bedrock Converse，temperature `0`、max tokens `600`、retries `0`；沒有新增 Python dependency，沿用既有 exact model／Guardrail runtime configuration。
- Runtime boundary：SSM shell 不需 source 整份設定；entrypoint 只從固定 `/etc/co-story/runtime.env` 讀取五個必要 Bedrock key，忽略且不輸出其他設定。
- Targeted verification：incident analyzer／adapter／entrypoint `13 passed`；affected suite `58 passed`。
- Full regression：Backend `343 passed, 8 skipped`。
- Sensitivity：暫時移除 `requires_human_approval=true` guard 後，目標測試精確失敗；mutation 已還原，targeted 與 full regression 重新全綠。
- Release artifact：從 detached exact commit `59f54586324427e94760899531c0104722050204` 建立 `tier1-20260824-59f5458`；archive 約 `148 KiB`，SHA-256 `50331286421507ba7639a5f2ab5e4eb2c51ec0cbb7d92e2ef19d7db4b3946d60`，本機 `shasum -c` 為 `OK`，並確認 archive 含三個 AIOps module。build 使用暫時 worktree，未納入工作樹中未提交的簡報。
- AWS zero-model deployment gate（2026-08-25）：使用者將 exact archive／checksum 上傳至既有 private artifact prefix，並透過 SSM Session 執行兩次 exact-object S3 read；`co-story.tar.gz: OK`。第一次 installer member 檢查因 `tar | grep -q` 在 `pipefail` 下觸發 SIGPIPE false negative，於安裝前安全停止；修正只讀檢查後部署成功，active release 為 `tier1-20260824-59f5458`，application／CloudWatch Agent／public edge 均為 `active`。
- Zero-model runtime result：固定 safe log 最近 `200` 行全部通過 allowlist（accepted `200`、discarded `0`）；runtime 五鍵只驗證存在與 parser，不輸出值；AIOps entrypoint、report schema 與 `requires_human_approval=true` guard 通過。`bedrock_invocations=0`、deployment gate exit `0`。
- Exactly-one Nova Lite gate（2026-08-25）：使用者另行核准後，由 SSM Session 在 active release 建立不可重跑 marker，再執行一次 AIOps entrypoint；retries `0`。回傳安全錯誤 `{"error":{"code":"SCHEMA_INVALID"}}`、exit `3`，代表模型輸出未通過固定 report contract；原始模型內容未輸出。application／CloudWatch Agent／public edge 在呼叫前後均為 `active`，沒有 recovery action 或第二次呼叫。
- Forced-tool remediation（2026-08-25）：AWS 官方資料確認 Nova Lite v1 不支援 Bedrock 原生 structured outputs，但支援 Converse client-side tool calling 與指定 `toolChoice.tool`。Red `dea39a2` 證明舊 adapter 無法解析 forced tool output 且仍接受 text JSON；Green `38296e7` 改以唯一、output-only 的 `submit_incident_report` tool schema 回傳報告，不提供 recovery tool，並拒絕 text JSON、錯誤工具、混合或多餘 content block。Targeted `6 passed`、AIOps affected `16 passed`；移除 single-block guard 的 sensitivity 精確失敗，還原後全綠。
- Regression／SDK contract：鎖定 botocore `1.43.69` 的本機 service model 確認支援 `toolConfig.toolChoice.tool`；Backend `346 passed, 8 skipped`。首次 full regression 因 repo `.venv` 不存在而在 collection 前缺少既有依賴，依鎖定 `requirements-dev.txt` 重建環境後通過，未修改 dependency lock。
- Replacement artifact：release `tier1-20260825-38296e7` 由 exact commit `38296e7f1e06d74a32ce551a62b064fe0dbac5af` 建立；archive `150625` bytes，SHA-256 `dacd7a168b49f294229aba0b881ac0a353095750d2145aed87b585c85155e47e`，本機 checksum `OK`，並確認包含 forced-tool adapter、AIOps entrypoint 與 release update installer。
- Replacement AWS zero-model gate：使用者透過 Console／SSM 上傳及讀取 exact archive／checksum，EC2 checksum `OK`；active release 更新為 `tier1-20260825-38296e7`，application／CloudWatch Agent／public edge 均為 `active`。以 fake Converse client 在 EC2 驗證 deployed adapter 強制唯一 `submit_incident_report` tool choice、固定 log parser 與人工批准 guard；最近 `200` 行 accepted `199`、discarded `1`，不合 allowlist 的一行未傳給模型。舊 exactly-one marker 仍保留，Bedrock invocations `0`，deployment gate exit `0`。
- Forced-tool exactly-one Nova Lite gate：使用者另行核准後建立新不可重跑 marker，以 active release 執行單次 Converse、retries `0`。固定 report schema 通過：summary／probable cause 判定系統正常，evidence 為 service active、scanned `200`／accepted `199`／discarded `1`、5xx `0`；recommended action `NO_ACTION`、`requires_human_approval=true`。application／CloudWatch Agent／public edge 呼叫前後均為 `active`，舊 marker保留、新 marker recorded，gate exit `0`。
- Human decision：使用者明確批准 `NO_ACTION`；因此沒有執行 SSM、restart、DB check 或任何 AWS 變更。此結果完成健康狀態 AIOps 判讀與人工批准 gate，並證明模型未在無 incident 時要求不必要修復。
- Bounded incident response gate：在 Alarm `OK`／Actions `No actions` 與三個 services active 的 preflight 下，使用者對固定安全 JSONL 寫入 exactly one `/tier1/aiops-incident-simulation` status `500`；marker `completed_exit=0`，唯讀 audit 確認最近 200 行 exact match `1`，未重複注入。Alarm 隨後已自動回到 `OK`，Last state update `2026-08-25 13:59:03`、Actions `No actions`；本輪 `In alarm` transition exact time 尚待從 history 補回。
- Incident-specific forced-tool analysis：在 synthetic event仍位於 bounded window時，使用者另行核准 exactly-one Nova Lite call、retries `0`。固定 schema成功，模型辨識 500 並建議 `CHECK_DATABASE`、`requires_human_approval=true`；application／CloudWatch Agent／public edge 前後 active，新 marker recorded。因輸入沒有 DB failure evidence，使用者明確拒絕 `CHECK_DATABASE`，改批准最小權限 `RUN_HEALTH_CHECK`。
- Approved controlled action：使用者在 Console 對唯一 EC2 target Rerun既有 `CoStoryHealthCheck` version `2`／default；Command 與 step 均為 `Success`、response code `0`，輸出 `service=active`、`live=200`、`ready=200`，Error `0`。沒有 restart、DB secret read、S3／CloudWatch output 或額外 Bedrock call。截圖：[`ssm-approved-health-check.png`](ssm-approved-health-check.png)。
- Result／residual risk：**PASS／BOUNDED AIOPS INCIDENT RESPONSE VERTICAL SLICE**。已完成 synthetic detection、forced-tool判讀、人工拒絕證據不足建議、人工批准較安全 action 與受控健康驗證；Alarm 自動恢復且 application未故障，因此這不是「實際服務中斷後 restart 修復」證據。正式 Tier 1 全項完成仍需依 checklist補 system metrics／dashboard等缺口，且不得重用或刪除既有 exactly-one markers。
