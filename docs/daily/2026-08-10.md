# 2026-08-10 本機 MVP 基礎任務

## 當日原定基礎目標

完成正式入口最後缺口：有效 session 的「繼續目前遊戲」、Play／Ending deep-link refresh，以及可返回首頁的 404。完成後直接推進後續本機 MVP 基礎工作，不先進行視覺美化或非必要重構。

## 完成狀態

- [x] Baseline：Backend `39 passed`、Frontend `51 passed`，工作樹乾淨。
- [x] `GET /api/v1/session/current` 提供匿名或有效 session 的安全摘要。
- [x] 依 `DRAFT／LOBBY／進行中／COMPLETED` 回傳 setup／lobby／play／ending route。
- [x] 首頁僅對有效 session 顯示「繼續目前遊戲」。
- [x] 失效 pointer 留在首頁並顯示原因，不回退至 Demo。
- [x] Play／Ending deep link 可重新整理；未知路由回傳可回首頁的 HTML 404。
- [x] `/demo` 仍使用隔離 Mock，未呼叫 session API。
- [x] 最終 regression：Backend `45 passed`、Frontend `56 passed`。
- [x] Browser Console `0 errors`，本機伺服器已停止。
- [x] 未登入或操作 AWS，費用影響為零。

## 超前後的基礎順序

1. 三個獨立玩家 session 完成正式一回合 Browser E2E。
2. PostgreSQL repository contract 與 process restart persistence。
3. LLM retry／schema invalid／內容拒絕與 deterministic fallback。
4. Polling offline／reconnected 與基本錯誤狀態。

不在此時安排動畫、配色微調或大型 `GamePage` 重構。
