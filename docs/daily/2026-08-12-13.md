# 2026-08-12–13 本機 MVP P0 收斂

## 排程目標

在 8/16 本機 MVP 里程碑前完成 Session lifecycle 與必要 Browser release gates。只處理 P0 正確性、安全與可驗收流程；不執行 AWS，也不做 P1 外觀優化或大型重構。

## 8/12 P0：Session expiry（已完成）

1. 完成核准 activity allowlist 的 Room／actor session refresh。（已完成）
2. 將 room、Host 與 Player expiry 接入 current read 與 mutation authorization，`None`／精確到期均 fail-closed。（已完成）
3. 驗證過期 read／mutation 的核准錯誤語意，不先洩漏 CSRF／version。（已完成）
4. 驗證 GET／polling、replay、拒絕與失敗 mutation 不延長期限。（已完成）
5. 完成 Backend regression、代表性 sensitivity 與同一份 Session R3 manifest。（已完成）

## 8/13 P0：Transfer 與 release gate

1. Host 發行 10 分鐘一次性 transfer code；新碼使舊未使用碼失效。
2. Atomic redeem／Player session rotate／舊 session revoke，涵蓋錯 room／player、過期、replay 與 concurrent redeem。
3. 完成房 7 天內只允許唯讀轉移；過期房禁止；房主自己的 Host session 保留並提示未移轉。
4. Browser 驗證 session expired、offline／reconnected、結局與 console 無未處理錯誤。
5. 更新本機 MVP Test Plan 與 CURRENT；只有 release gate 全綠才標示本機 MVP 完成。

## 明確不做（P1）

- 動畫、視覺 polish、非必要 responsive 微調。
- 大型架構重構、Docker image 最佳化、multi-process 擴展。
- 關鍵字世界生成等非 release blocker 功能。
- AWS workload、production deploy 或 GitHub release。
